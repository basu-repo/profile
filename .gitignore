# Database credentials. Never commit this -- copy config/config.example.php
# to config/config.php on each machine and fill it in there.
config/config.php
/config.php

# Runtime data written by the application
storage/*.log
uploads/

# Local tooling and OS noise
.vscode/
.codex
.DS_Store
Thumbs.db
*.swp

# Documents such as the CV and paper PDFs stay out of the repository. They can
# hold personal details, and nothing in the site reads them from here.
*.pdf
*.PDF
*.doc
*.DOC
*.docx
*.DOCX
*.odt
*.rtf
